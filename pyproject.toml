[build-system]
requires = ["setuptools>=56", "wheel", "setuptools_scm[toml]>=10.1.2"]
build-backend = "setuptools.build_meta"

[tool.black]
skip-string-normalization = false

[tool.setuptools_scm]

[pytest.enabler.mypy]
addopts = "--mypy"

[pytest.enabler.cov]
addopts = "--cov"
